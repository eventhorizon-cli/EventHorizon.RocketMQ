# Remoting benchmark baseline: <label>

## Source

- Recorded (UTC): `<YYYY-MM-DDTHH:mm:ssZ>`
- Commit: `<full commit SHA>`
- Working tree: `<clean, or describe intentional changes>`
- Comparison target: `<commit SHA or record path, if applicable>`

## Environment

- Machine or runner: `<stable identifier>`
- OS and architecture: `<value>`
- CPU and logical core count: `<value>`
- Memory: `<value>`
- Power mode and relevant background-load notes: `<value>`
- .NET SDK and runtime: `<value>`
- BenchmarkDotNet job/runtime: `<value>`

## Commands

Run from the repository root and preserve the exact commands, including filters and additional BenchmarkDotNet
arguments.

```shell
<exact command(s) used for this record>
```

## Results

Paste the complete Markdown result table for every selected benchmark from
`BenchmarkDotNet.Artifacts/results/*-report-github.md`. Keep parameter, mean, error, standard deviation, ratio, and
allocation columns intact.

<!-- Paste actual BenchmarkDotNet result tables here. -->

## Warnings and notes

<!-- Record BenchmarkDotNet warnings, outliers, launch failures, thermal events, and other comparison constraints. -->
